<form method="post" action="{{ path('app_annonce_deletefront', {'id': annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn_3"><i class="fas fa-trash-alt" aria-hidden="true"></i></button>
</form>
