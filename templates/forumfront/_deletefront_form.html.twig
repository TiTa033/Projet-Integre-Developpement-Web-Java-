<form method="post" action="{{ path('app_forumfront_delete', {'id': forum.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ forum.id) }}">
    <button class="genric-btn primary-border circle">Delete</button>
</form>
