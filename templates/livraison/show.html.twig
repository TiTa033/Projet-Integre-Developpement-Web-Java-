{% extends 'BaseBackOffice.html.twig' %}



{% block content %}
    <h1>Livraison</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ livraison.id }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ livraison.Statut }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ livraison.Adresse }}</td>
            </tr>
            <tr>
                <th>Cout</th>
                <td>{{ livraison.Cout }}</td>
            </tr>
            <tr>
                <th>Methode</th>
                <td>{{ livraison.Methode }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ livraison.Date }}</td>
            </tr>
            <tr>
                <th>Commentaires</th>
                <td>{{ livraison.Commentaires }}</td>
            </tr>
        </tbody>
    </table>

    
    
      
         
<div class="btn-group mb-3" role="group" aria-label="Basic example">
                           <a href="{{ path('app_livraison_index') }}" class="btn btn-secondary">back to list</a>
                             <a href="{{ path('app_livraison_edit', {'id': livraison.id}) }}" class="btn btn-secondary">edit</a>
                            {{ include('livraison/_delete_form.html.twig') }}
                        </div>

{% endblock %}
